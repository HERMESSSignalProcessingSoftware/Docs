\begin{figure}[htb] 
	\begin{center}
		\begin{tikzpicture}[node distance = 4cm, black, ultra thick]
			\node[state] (start) at (0,0) {Start};
			\node[state, above right of = start, node distance = 8cm] (ResyncEvent) {Resync Event};
			\node[state, below of = ResyncEvent, node distance = 8cm] (S11) {S11};
			\node[state, below left of = S11] (S1) {S1};
			\node[red!50, state, left of = S1] (Error1) {Error 1};
			\node[state, below of = Error1] (End1) {End 1};
			% Draw lines 
			\coordinate (tempStart) at ($(start) - (2,-1)$);
			\path[->, draw, black] (tempStart) -- node[sloped, anchor=center, above, align=left]{$\overline{reset}$} (start);
			\path[->] (start) edge[bend left] node[sloped, anchor=center, above,text width=3.5cm, align=left]{\small{\# Request Resync = $X_1$\\ \& ResyncTimer = 0}} (ResyncEvent);
			\path[->] (ResyncEvent) edge[bend left] (start);
			\path[->] (start) edge[bend left] node[sloped, anchor=center, above,text width=3.2cm, align=left] {\small{\# new Avail $>$ 0}} (S1);
			\path[->] (S1) edge[loop left] node[sloped, anchor=center, above, align = left] {all other} (S1);
			\path[->] (S1) edge[bend left] node[sloped, anchor=center, below, text width=3.2cm,align = left] {\small{\# New Avail = 6 \\ ODER\\ $~$\\ \# New Avail $\ge~X_2$ \\ AND \\ WaitingTimer = 0 }} (End1);
		\end{tikzpicture}
	\end{center}
\end{figure}